/AGENTS.md

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# Local ticket roadmap (not versioned)
TICKETS-0S.md
maquettes/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml

# Poetry (in-project venv in src/backend)
src/backend/.venv/

# Node / React UI (react)
node_modules/
react/dist/
react/.vite/
react/coverage/
react/*.tsbuildinfo
react/.env

# Flutter (flutter)
flutter/coverage/
flutter/build/
flutter/.dart_tool/

# Rust (rust) — cargo build artifacts, never versioned. Cargo.lock IS
# committed (cdylib/application artifact — reproducible builds).
rust/target/
rust/**/*.rs.bk

hexa_guard.py

# Backend env secrets (copy src/backend/.env.example -> .env)
src/backend/.env
src/backend/.env.*
!src/backend/.env.example

# React env vars (copy react/.env.example -> .env)
react/.env.*
!react/.env.example

# Runtime logs (generated by setup_logging — never versioned)
src/backend/logs/
*.log
.env

# graphify (generated knowledge graph + opencode integration)
graphify-out/
.opencode/plugins/graphify.js
.opencode/opencode.json
.graphifyignore
